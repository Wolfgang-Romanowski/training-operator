apiVersion: v1
kind: Service
metadata:
  name: training-operator-controller-manager
  namespace: opendatahub
  labels:
    app.kubernetes.io/name: training-operator-controller-manager
    app.kubernetes.io/component: metrics
    app.kubernetes.io/part-of: training-operator
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    control-plane: controller-manager
    app.kubernetes.io/name: training-operator